syntax = "proto3";

message N {
    int32 starttime = 1;
    int32 endtime = 2;
    bool longnote = 3;
    bool parallel = 4;
}
message C {
    repeated N notes = 1;
}
message M {
    repeated C channels = 1;
    int32 speed = 2;
}